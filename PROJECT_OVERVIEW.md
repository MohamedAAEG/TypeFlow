# TypeFlow — نظرة عامة على المشروع وملخّص آخر التحديثات

> منصّة عربية لتعلّم الكتابة على لوحة المفاتيح وتعلّم الإنجليزية، تعمل في المتصفح
> مع **قاعدة بيانات Supabase** كمصدر موثوق لبيانات المستخدمين والمحتوى.

آخر تحديث: 16 يونيو 2026 · الموقع المباشر: **https://type-flow-rust.vercel.app**

---

## 1. ما هو التطبيق؟
تطبيق ويب تعليمي موجّه بالـ Onboarding يجمع:
1. **تحسين سرعة ودقّة الكتابة** (عربي/إنجليزي) بقياس WPM والدقّة.
2. **تعلّم الإنجليزية** أثناء الكتابة: نطق حيّ، ترجمة الكلمة والجملة، قائمة تعلّم.
3. **تحديات القواعد (Grammar Quests)** — دروس أزمنة تفاعلية بثلاث مستويات، يؤلّفها الأدمن بالكامل.

الواجهة عربية (RTL)، بنظام Design Tokens، وضع فاتح/داكن، وتخصيص مظهر واسع.

---

## 2. البنية التقنية
| الطبقة | التفاصيل |
|---|---|
| **الواجهة** | HTML/CSS/JS خام (Vanilla)، بلا إطار ولا خطوة build. كل المنطق في `app.js` داخل IIFE واحد. |
| **الـ backend** | **Supabase** (Postgres + RLS + دوال RPC + Edge Function + Storage). مشروع `TypeFlow-DB` (ref `waqujaacvcodnpxsuooz`). |
| **الاستضافة** | **Vercel** (مشروع `type-flow`)، نشر تلقائي من فرع `main` على GitHub. |
| **مكتبات CDN** | SheetJS (xlsx) لاستيراد/تصدير Excel. |
| **PWA** | `manifest.json` + `sw.js` (network-first). **مهم:** أي تعديل أصول يتطلّب رفع رقم `CACHE` في `sw.js` (وصلنا `typeflow-v9`). |

**مستودع العمل الرسمي:** `C:\Users\moham\Documents\GitHub\TypeFlow` (المربوط بـ Vercel). (`D:\typeflow` نسخة قديمة منفصلة.)

### الملفات الرئيسية
```
index.html        # الهيكل والأقسام والنوافذ
style.css         # كل الأنماط + Design Tokens
app.js            # كل المنطق (الحالة، التدريب، المساعدة، الأدمن، Supabase، القواعد)
english-data.js   # محتوى الإنجليزية حسب السبب/المستوى
typing-data.js    # تمارين الكتابة
words-db.js       # ترجمات الكلمات + مسارات صوت محلية
grammar-data.js   # احتياطي offline لتحديات القواعد فقط (المصدر الأساسي صار Supabase)
audio.js          # مؤثرات صوت المفاتيح
sw.js / manifest.json / icon.svg   # PWA
```

---

## 3. قاعدة البيانات (Supabase)
كل الجداول **RLS مقفل** والوصول عبر دوال `SECURITY DEFINER` فقط (لا وصول مباشر).

| الجدول | الغرض |
|---|---|
| `users` | حسابات المستخدمين (كلمات المرور bcrypt عبر pgcrypto). |
| `user_sessions` | توكنات الجلسات (تُصدر عند الدخول/التسجيل). |
| `user_state` | بيانات كل مستخدم (JSON) — البروفايل/التقدّم/السجل/قائمة التعلم/المكتبة/المظهر/تقدّم القواعد. |
| `grammar_quests` | قواعد Grammar Quests الديناميكية (concept/sentences/questions/settings كـ JSON). |
| `tracks`/`lessons`/`lesson_words`/`user_typing_attempts`/`user_word_srs` | جداول مصمّمة سابقًا (غير مستخدمة فعليًا حاليًا). |

**دوال RPC الأساسية:**
- **الحساب:** `app_signup`, `app_login` (يُرجعان token), `app_change_password`, `app_is_admin` (داخلية), `app_list_users`/`app_delete_user` (للأدمن).
- **بيانات المستخدم:** `us_load(token)`, `us_save(token, data)`.
- **القواعد:** `gq_list_quests` (عامة), `gq_upsert_quest`/`gq_delete_quest`/`gq_reorder` (للأدمن).

**Edge Function:** `gq-media-upload` — رفع صور/صوت مقيّد بكلمة مرور الأدمن (service-role داخليًا) إلى bucket عام **`grammar-media`**.

---

## 4. الميزات

### تجربة الكتابة
- وضع Zen/تركيز، قياس فوري (WPM/دقّة/أخطاء/زمن)، إعادة 1–100، أحجام متعددة، تقدّم تلقائي، إيقاف عند الخمول، اختصارات Alt، إشعارات Toast، مؤشّرات تقدّم.
- **✅ التساهل في التصحيح (جديد):** مفتاح في "إعدادات العرض" يتجاهل **حالة الأحرف** (كبير/صغير) و**علامات الترقيم** (يمكن تخطّيها). يعمل عبر محاذاة ذكية؛ الوضع الصارم يبقى الافتراضي.

### تعلّم الإنجليزية
- نطق الكلمة النشطة (Web Speech، لهجة US/GB) + شريط سرعة، ترجمة الكلمة سياقية وترجمة الجملة، لغة ترجمة قابلة للاختيار، قائمة تعلّم شخصية.

### تحديات القواعد (Grammar Quests) — ديناميكي بالكامل
رحلة إجبارية بثلاث مستويات لكل قاعدة: **الشرح** (بلوكات: نص ملوّن/صورة/فاصل/شكل) ← **الذاكرة العضلية** (كتابة جمل، تعليل لكل جملة، صوت اختياري) ← **معركة النهاية** (أسئلة اختيار/إكمال فراغ مع تغذية راجعة للصح والخطأ). شريط تقدّم 1/3→3/3، والإكمال يُحفظ لكل قاعدة.
- **التأليف للأدمن:** تبويب "تحديات القواعد" في لوحة المدير — إنشاء/تعديل/حذف/ترتيب، محرّر بلوكات، محرّر جمل وأسئلة، **استيراد Excel**، **رفع صور وصوت**، وضبط توقيت ظهور الشرح والتشغيل التلقائي.

### الحساب والمكتبة والتحليلات
- تسجيل/دخول، 5 تبويبات بروفايل، مجلّدات شخصية + رفع Excel، تبويب أداء (إحصائيات/سلسلة أيام/رسم SVG)، تصدير/استيراد، **لوحة أدمن** (المحتوى/المستخدمون/القواعد).

---

## 5. تدفّق البيانات (مهم)
- **الحسابات والمحتوى مشتركة على Supabase** — تظهر عبر كل الأجهزة.
- **بيانات المستخدم = القاعدة هي المصدر:** عند الدخول/بدء التشغيل تُسحب من `user_state`، وأي تغيير يُكتب تلقائيًا للقاعدة (اعتراض واحد لطبقة الحفظ + debounce). `localStorage` صار **كاش محلي فقط**. (الزائر غير المسجّل يبقى محليًا.)
- التوكن داخل `currentUser` المحفوظ محليًا؛ يُمسح الكاش عند الخروج.

---

## 6. ملخّص جلسة التطوير (يونيو 2026)
سلسلة تحسينات كبرى نُفّذت ورُفعت على `main` (Vercel نشرها):
1. **حسابات على Supabase** (`426bc35`) — حلّ عدم ظهور المستخدمين الجدد للأدمن عبر الأجهزة (كانت في localStorage فقط).
2. **Grammar Quests** ثابت ثم **ديناميكي بالكامل** (`21a5256` → `264ad51`, `2fb81a9`, `1a9c333`, `9905412`): جدول + دوال + محرّر بلوكات + استيراد Excel + رفع وسائط عبر Edge Function وbucket.
3. **بيانات المستخدم على Supabase** (`5d0306d`) — حلّ خلل "محفوظ على جهاز ومفقود على آخر"؛ القاعدة صارت المصدر الموثوق.
4. **مفتاح التساهل في التصحيح** (`c8b88b8`) — تجاهل حالة الأحرف وعلامات الترقيم.

---

## 7. قيود معروفة وخطوات تالية مقترحة
- **إحصائيات الأدمن** (محاولات/WPM) ما زالت تُقرأ من تخزين الأدمن المحلي؛ يمكن قراءتها من `user_state` لكل مستخدم لجعلها دقيقة عبر الأجهزة.
- **كلمة مرور الأدمن** الافتراضية (`admin`/`admin123`) عامة — يُفضّل تغييرها.
- **المستخدمون القدامى** يُرحَّلون تلقائيًا إلى القاعدة عند أول تسجيل دخول بعد التحديث.
- التساهل في التصحيح مطبّق على محرّك صفحة التمرين؛ يمكن لاحقًا تطبيقه على كتابة "تحديات القواعد" (المستوى 2).
- بقيت 3 ملفات اختبار ضئيلة في bucket `grammar-media` (يمكن حذفها من لوحة Supabase Storage).

---

## 8. اتصالات الجلسة (للرجوع)
- **Supabase MCP** (مشروع `waqujaacvcodnpxsuooz`) · **Vercel MCP** (team `team_fX0JDHeJMcCkz1Qur4VT82RQ`، مشروع `prj_qEMUrxe1WArqxB5lmZ5RMFCmuRBT`).
- **مفتاح Supabase العلني (publishable)** مضمّن في `app.js` — آمن للعميل (الحماية في RLS + الدوال).
